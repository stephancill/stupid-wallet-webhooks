{
  "name": "address-notifications",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 migrations apply address-notifications-db",
    "db:migrate:local": "wrangler d1 migrations apply address-notifications-db --local",
    "op": "bun scripts/operator.mts",
    "test": "bun test",
    "lint": "bunx oxlint src scripts test",
    "format": "bunx oxfmt --write .",
    "check": "bun run lint && bun run format"
  },
  "dependencies": {
    "@hono/zod-validator": "^0.7.1",
    "hono": "^4.11.11",
    "viem": "^2.28.9",
    "zod": "^3.24.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260417.0",
    "@types/bun": "^1.4.0",
    "typescript": "^5.8.3",
    "wrangler": "4.126.0"
  }
}
